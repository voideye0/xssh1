- pipeline: "gh"
  events:
  - type: "PUSH"
    refs:
    - "refs/heads/main"
  fail_on_prepare_env_warning: true
  resources: "NANO"
  actions:
  - action: "Build Docker image"
    type: "DOCKERFILE"
    do_not_prune_images: true
